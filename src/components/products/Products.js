import { ProductForm } from "./ProductForm";
import { ProductList } from "./ProductList";

export const Products = () => {
    return (
        <div>
            <ProductForm/>
            <ProductList/>
        </div>
    );
}